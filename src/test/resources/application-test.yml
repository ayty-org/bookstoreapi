spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/bookstoreapi_tests
    password: a
  flyway:
    baseline-on-migrate: 'false'
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate:
      ddl-auto: create-drop
